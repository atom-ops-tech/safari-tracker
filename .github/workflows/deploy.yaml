name: Deploy to Safari App

on:
  push:
    branches:
      - main

permissions:
  id-token: write  # Required for OIDC authentication
  contents: read   # Required to checkout repository

jobs:
  build-and-deploy:
    name: Copy and deploy
    runs-on: ubuntu-latest
    
    env:
      AWS_REGION: us-west-2
      S3_BUCKET: safari-tracker-atom-ops-homepage-011015904112
      CLOUDFRONT_DISTRIBUTION_ID: EG87EYFF8V3JA

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::011015904112:role/gha-safari-tracker-deploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy to S3 with optimized settings
        run: |
          aws s3 cp index.html s3://${{ env.S3_BUCKET }}/index.html
          aws s3 cp timbafacts/index.html s3://${{ env.S3_BUCKET }}/timbafacts/index.html
          aws s3 cp robots.txt s3://${{ env.S3_BUCKET }}/robots.txt

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"